#include<stdio.h>
void main(){
    int a,b,sum,product,diff;
    printf("Please insert two number:");
    scanf("%d%d",&a,&b);
    sum = a+b;
    product = a*b;
    diff = a-b;
    printf(" sum of the numbers: %d",sum);
    printf("\n product of the numbers: %d",product);
    printf("\n difference of the numbers: %d",diff);

}

